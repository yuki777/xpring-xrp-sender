# Install
```
npm install
```

# Send XRP on Testnet
```
cp .env.testnet .env # => Edit .env
node send-xrp.js --help
```

# Send XRP on Mainnet
```
cp .env.mainnet .env # => Edit .env
node send-xrp.js --help
```
